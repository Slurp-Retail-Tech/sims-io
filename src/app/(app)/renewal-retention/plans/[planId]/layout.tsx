import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Renewal – Plan Detail",
}

export default function PlanDetailLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
