import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Renewal – Settings",
}

export default function RenewalSettingsLayout({ children }: { children: React.ReactNode }) {
  return children
}
