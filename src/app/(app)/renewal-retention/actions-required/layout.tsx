import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Renewal – Actions Required",
}

export default function ActionsRequiredLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
