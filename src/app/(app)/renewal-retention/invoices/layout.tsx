import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Renewal – Invoices",
}

export default function RenewalInvoicesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
