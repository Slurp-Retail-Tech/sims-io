---
version: "2.1.14"
date: "2026-06-08"
title: "Clearer diagnostics for franchise import auth failures"
summary: "When the franchise import fails with an authentication error, the server log now records non-secret diagnostics that pinpoint why the POS API rejected the session."
new: []
improved:
  - title: "Actionable logging when the POS import call is rejected"
    detail: "A 401 from the franchise import endpoint only happens after a successful login, so it now logs the request and final URLs, whether the request was redirected or protocol-downgraded (which strips the Authorization header), whether a session cookie was present, and a masked token preview. This distinguishes a redirect-stripped token from a genuinely invalid one. Tokens are masked and never logged in full."
fixed: []
breaking_changes: ""
---
