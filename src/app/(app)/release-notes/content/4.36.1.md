---
version: "4.28.1"
date: "2026-09-23"
title: "Pages load without a hydration error"
summary: "Every signed-in page was throwing away its server-rendered HTML on load and redrawing from scratch in the browser. The sidebar and the ticket date filter now render the same on the server as in the browser, so pages hydrate cleanly and the stray console errors are gone."
new: []
improved: []
fixed:
  - title: "Sidebar matches between server and browser"
    detail: "The sidebar built its menu from a copy of your account cached in the browser, which the server can't see, so the first render always disagreed and the page was redrawn. It now starts from the account the server has already verified. The menu, the workspace switcher and profile updates behave exactly as before."
  - title: "Remembered workspace and ticket date range"
    detail: "The workspace chosen in the sidebar and the date range on Merchant Success Tickets are now read on the server, so they appear on first load instead of causing a mismatch."
  - title: "Script-tag warning"
    detail: "The \"Encountered a script tag\" console error was a side effect of the redraw above: the theme script only ran again because the page was being rebuilt in the browser. With hydration fixed it no longer appears, and light/dark theme switching is unchanged."
breaking_changes: ""
---
