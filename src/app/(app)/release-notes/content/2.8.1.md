---
version: "2.8.1"
date: "2026-06-29"
title: "Fix demo form submission failures"
summary: "Demo form submissions were failing on environments whose database predated the lead-form changes, because the leads table still required the removed email and business_name columns. A migration relaxes those columns to nullable so submissions succeed again."
new: []
improved: []
fixed:
  - title: "Demo form submissions failing with a database error"
    detail: "Both demo forms returned an error on submit (ER_NO_DEFAULT_FOR_FIELD: \"Field 'email' doesn't have a default value\") on databases where the leads.email and leads.business_name columns were still NOT NULL. Migration 010 makes both columns nullable to match the current schema; run it against each environment's database."
breaking_changes: ""
---
