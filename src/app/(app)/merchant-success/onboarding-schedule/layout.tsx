import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Onboarding Schedule",
}

export default function OnboardingScheduleLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
